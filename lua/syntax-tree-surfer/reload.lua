return function()
	-- for n, _ in pairs(package.loaded) do
	-- 	print("checking", n)
	-- 	if n:match("syntax-tree-surfer") then
	-- 		print("unloading", n)
	-- 		package.loaded[n] = nil
	-- 	end
	-- end
end
